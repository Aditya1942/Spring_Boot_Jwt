
jwt.secret=javainuse

#spring.doc.swagger-ui.path=/swagger-ui

springdoc.api-docs.path =/swagger-ui



## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.jpa.database=mysql
spring.datasource.url=jdbc:mysql://localhost:3306/angular_admin?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=#Yv9pg92y

## Hibernate Properties

spring.datasource.driver-class-name= com.mysql.jdbc.Driver
spring.jpa.show-sql=true

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL55Dialect
spring.jpa.hibernate.ddl-auto=update

# Initialize the datasource with available DDL and DML scripts
spring.datasource.initialization-mode=always

## Jackson Properties
#spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
#spring.jackson.time-zone= UTCson.time-zone= UTC